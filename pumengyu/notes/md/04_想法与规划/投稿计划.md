# 投稿计划（目标：二三区）

> 更新日期：2026-06-13  
> 当前最优模型：**MLAUNet_MoE_SizeOV4**（Overall 0.8330 / Tumor Dice 0.7146）  
> 目标期刊：CMPB / BSPC / CMIG / Neurocomputing（Q2/Q3）

---

## 硬件资源

| 资源 | 状态 |
|------|------|
| GPU 0 (RTX 4090 D 24GB) | 占用 ~6.4GB，训练中 |
| GPU 1 (RTX 4090 D 24GB) | 占用 ~6.7GB，训练中 |
| 磁盘可用 | **235G（偏紧，注意清理中间权重）** |
| 内存 | 61GB，21GB 可用 |

### 环境变量（已确认设置正确）

```bash
nnUNet_raw=/home/PuMengYu/nnUNet_workspace/raw
nnUNet_preprocessed=/home/PuMengYu/nnUNet_workspace/preprocessed
nnUNet_results=/home/PuMengYu/nnUNet_workspace/results_v2
```

---

## 关于数据划分

`splits_final.json` 只含 train(92) + val(13) 共 105 个 case，**26 个 test case 未写入**（存在 `split_info_712.json`）。所有对比方法直接读同一份 `splits_final.json`，训练时天然排除 test 26 cases，无需任何额外操作。

论文 Experiments 节写一句即可：
> *"We adopt a fixed 7:1:2 train/validation/test split (seed=42) applied consistently across all compared methods."*

**不需要做 5 折交叉验证**（Q2/Q3 不强制，对比方法全部 5 折代价过高）。

---

## 整体路线（按优先级）

```
Phase 1  外部验证（最快产出，GPU 一空立刻跑）
Phase 2  对比实验（主要时间，2~4 周）
Phase 3  统计检验 + 可视化（3~5 天）
Phase 4  写作投稿（3~4 周）
```

---

## Phase 1：外部验证（ircadb_full）

**目的**：证明泛化性，审稿人最看重的加分项。  
**数据**：`/home/PuMengYu/nnUNet_workspace/external_val/ircadb_full/`，20 cases，images + labels 齐全。  
**评估脚本**：`pumengyu/ext_val/02_eval_ircadb.py`（已有）  
**状态**：🚀 2026-06-12 晚 23:00 已启动推理

### 推理命令

```bash
# MoE_SizeOV4 推理（不加 --disable_tta，与内部测试保持一致）
CUDA_VISIBLE_DEVICES=1 nnUNetv2_predict \
  -i /home/PuMengYu/nnUNet_workspace/external_val/ircadb_full/images \
  -o /home/PuMengYu/nnUNet_workspace/external_val/ircadb_full/predictions/MoE_SizeOV4 \
  -d 003 -c 3d_fullres \
  -tr nnUNetTrainer_MLAUNet_MoE_SizeOversampleV4 \
  -f 0

# Baseline 推理（作为 ircadb 上的对照）
CUDA_VISIBLE_DEVICES=1 nnUNetv2_predict \
  -i /home/PuMengYu/nnUNet_workspace/external_val/ircadb_full/images \
  -o /home/PuMengYu/nnUNet_workspace/external_val/ircadb_full/predictions/Baseline \
  -d 003 -c 3d_fullres \
  -tr nnUNetTrainer_Baseline \
  -f 0
```

### 评估

```bash
python pumengyu/ext_val/02_eval_ircadb.py
```

### 步骤

- [x] **1.0** 确认 ircadb_full images + labels 齐全（20 cases ✅）
- [ ] **1.1** MoE_SizeOV4 推理完成
- [ ] **1.2** 运行 02_eval_ircadb.py，记录 Liver Dice / Tumor Dice / 误报率
- [ ] **1.3** Baseline 推理 + 评估（对照）
- [ ] **1.4** 将 ircadb 结果补充到汇总分析 v2

---

## Phase 2：对比实验

### 方法选择与实现状态

| 方法 | 技术路线 | 架构文件 | Trainer 类 | DS | 实现状态 |
|------|---------|---------|-----------|-----|---------|
| **MedNeXt-L** | ConvNeXt 风格 3D UNet | `architectures/mednext.py` | `nnUNetTrainer_MedNeXt` | ✅ 5层 | ✅ 验证通过 |
| **SwinUNETR-B** | Swin Transformer | `architectures/swinunetr.py` | `nnUNetTrainer_SwinUNETR` | ❌ 关闭 | ✅ 验证通过 |
| **nnFormer** | 3D Swin Transformer UNet | `architectures/nnformer.py` | `nnUNetTrainer_nnFormer` | ❌ 关闭 | ✅ 验证通过 |

> SwinUNETR 和 nnFormer 的 DS 均未实现（原版如此），trainer 中关闭 DS，对比仍公平（同一数据划分、同一 loss 协议）。

### 训练命令

```bash
# MedNeXt-L（等 GPU0 空闲后）
CUDA_VISIBLE_DEVICES=0 nnUNetv2_train Dataset003_Liver 3d_fullres 0 \
  -tr nnUNetTrainer_MedNeXt

# SwinUNETR-B（等 GPU1 空闲后）
CUDA_VISIBLE_DEVICES=1 nnUNetv2_train Dataset003_Liver 3d_fullres 0 \
  -tr nnUNetTrainer_SwinUNETR

# nnFormer（等任意 GPU 空闲后）
CUDA_VISIBLE_DEVICES=1 nnUNetv2_train Dataset003_Liver 3d_fullres 0 \
  -tr nnUNetTrainer_nnFormer
```

### GPU 排期

```
当前：SizeOV5（GPU0+1 各一个进程）
↓ SizeOV5 结束后
GPU0 → MedNeXt
GPU1 → SwinUNETR
↓ 完成后
GPU0 or 1 → nnFormer
```

### 步骤

- [x] **2.0** 安装 mednextv1（含 nnFormer）：`pip install git+https://github.com/MIC-DKFZ/MedNeXt.git`
- [x] **2.1** 实现 MedNeXt-L：`architectures/mednext.py` + `nnUNetTrainer_MedNeXt` ✅
- [x] **2.2** 实现 SwinUNETR-B：`architectures/swinunetr.py` + `nnUNetTrainer_SwinUNETR` ✅
- [x] **2.3** 实现 nnFormer：`architectures/nnformer.py` + `nnUNetTrainer_nnFormer` ✅
- [x] **2.4** MedNeXt-L 训练 fold_0（约 2~3 天）
- [x] **2.5** SwinUNETR-B 训练 fold_0（约 2~3 天）
- [ ] **2.6** nnFormer 训练 fold_0（约 2~3 天）
- [ ] **2.7** 三个方法推理 test 26 cases + 生成 test_report

---

## Phase 3：统计检验 + 可视化

- [ ] **3.1 Wilcoxon 检验**：MoE_SizeOV4 vs 每个 baseline，26 cases per-case Dice，报 p 值
  - p < 0.05 → 显著，论文直接写
  - p ≥ 0.05（26 cases 样本量小有此风险）→ 报 effect size (Cohen's d) 代替
- [ ] **3.2 可视化**（至少 3 组对比图）
  - 亮点：liver_75（极小肿瘤，Dice 0.8275）、liver_68（极小，0.8749）
  - 难点：liver_127（完全漏检，诚实展示 failure case）
  - 无肿瘤：liver_89 / liver_91（0 体素误报，与 baseline 对比）
- [ ] **3.3 消融表整理**：nnUNet → MLAUNet → +MoE → +SizeOV4，四行标准消融表

---

## Phase 4：写作

### 论文结构（参考 CMPB/BSPC 格式）

1. **Introduction**：肝脏肿瘤分割难点（小肿瘤、假阳性），现有方法不足
2. **Related Work**：nnUNet 系列 / Transformer 在医学分割 / 过采样策略
3. **Method**：MLA 架构 + MoE 门控 + SizeOversampleV4，三节分写
4. **Experiments**
   - 数据集：LiTS internal 7:1:2 + 3D-IRCADb 外部验证
   - 对比实验：Baseline / MedNeXt / SwinUNETR / nnFormer / 本方法
   - 消融实验：四行表
   - 统计检验
   - 可视化
5. **Discussion**：liver_127 failure case 分析，局限性，未来方向
6. **Conclusion**

### 目标期刊优先级

| 期刊 | 分区 | 特点 | 审稿周期 |
|------|------|------|---------|
| **CMPB** (Computers in Biology and Medicine) | Q2 | 医学图像主流，接受率相对友好 | 2~3 月 |
| **BSPC** (Biomedical Signal Processing and Control) | Q2 | 速度稍快 | 1~2 月 |
| **CMIG** (Computerized Medical Imaging and Graphics) | Q2 | 图像质量要求高 | 2~3 月 |
| **Neurocomputing** | Q2 | 偏方法，接受率较高 | 2~3 月 |

**建议首投 CMPB 或 BSPC**。

---

## 关键检查清单（投稿前）

- [ ] 外部验证（ircadb 20 cases）有结果
- [ ] 3 个 published baseline 在同一测试集上的数字（MedNeXt / SwinUNETR / nnFormer）
- [ ] 消融实验四行表（每步有增益）
- [ ] Wilcoxon 检验 or effect size
- [ ] 可视化图（success + failure case）
- [ ] TRAINERS.md 和代码注释整理好（审稿后可能要求公开代码）

---

## 当前实验状态

| 实验 | 状态 | 备注 |
|------|------|------|
| MLAUNet_MoE_SizeOV4 | ✅ 完成 | 当前最优，Overall 0.8330 / Tumor Dice 0.7146 |
| MLAUNet_MoE_SizeOV5 | 🚀 训练中 | 均匀 3× 过采样，结果待出 |
| Tr_Stage2_FPSup_v2 | ⏳ 待跑 test_report | 训练完但未评估 |
| ircadb 外部验证 | 🚀 推理中（2026-06-12 23:00 启动） | 评估完立刻出结果 |
| MedNeXt-L | ✅ 训练完成 | `nnUNetTrainer_MedNeXt` |
| SwinUNETR-B | ✅ 训练完成 | `nnUNetTrainer_SwinUNETR` |
| nnFormer | ⏳ 代码 ✅，等 GPU 空闲后训练 | `nnUNetTrainer_nnFormer` |
