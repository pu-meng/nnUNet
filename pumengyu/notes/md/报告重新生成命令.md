# 报告重新生成命令

> 工作目录统一：`cd /home/PuMengYu/nnUNet`
>
> **当前报告节结构（最新版）**：肝脏Dice → 无肿瘤误报 → 综合指标 →
> FPV/FNV → 病灶级检出率 → **连通域阈值分析（Q1/Q2/Q3）** → 大小分组 → Per-Case分级
>
> eval_fold_report.py 已更新（2026-05-30），新增了连通域阈值分析节。

---

## 1. 补生成缺失报告（最高优先级）

### V3 test_report_custom.txt（推理完成但报告被中断）
```bash

cd /home/PuMengYu/nnUNet && python pumengyu/tools/gen_test_report.py \
  --trainer nnUNetTrainer_SizeOversampleV3 \
  --dataset Dataset003_Liver \
  --fold    0

```

---

## 2. 补充新节到旧报告（validation report_custom.txt）

> Baseline 和 V2 的 validation 报告是旧格式，缺 FPV/FNV、病灶检出率、连通域分析三节。
> V3 validation 报告有前两节，但缺连通域分析节。

### Baseline — validation report
```bash
cd /home/PuMengYu/nnUNet && python pumengyu/tools/analyasis/eval_fold_report.py \
  --val_dir /home/PuMengYu/nnUNet_workspace/results_v2/Dataset003_Liver/nnUNetTrainer_Baseline__nnUNetPlans__3d_fullres/fold_0/validation \
  --gt_dir  /home/PuMengYu/nnUNet_workspace/preprocessed/Dataset003_Liver/gt_segmentations \
  --img_dir /home/PuMengYu/nnUNet_workspace/raw/Dataset003_Liver/imagesTr \
  --no_vis
```

### SizeOversampleV2 — validation report
```bash
cd /home/PuMengYu/nnUNet && python pumengyu/tools/analyasis/eval_fold_report.py \
  --val_dir /home/PuMengYu/nnUNet_workspace/results_v2/Dataset003_Liver/nnUNetTrainer_SizeOversampleV2__nnUNetPlans__3d_fullres/fold_0/validation \
  --gt_dir  /home/PuMengYu/nnUNet_workspace/preprocessed/Dataset003_Liver/gt_segmentations \
  --img_dir /home/PuMengYu/nnUNet_workspace/raw/Dataset003_Liver/imagesTr \
  --no_vis
```

### SizeOversampleV3 — validation report（补连通域分析节）
```bash
cd /home/PuMengYu/nnUNet && python pumengyu/tools/analyasis/eval_fold_report.py \
  --val_dir /home/PuMengYu/nnUNet_workspace/results_v2/Dataset003_Liver/nnUNetTrainer_SizeOversampleV3__nnUNetPlans__3d_fullres/fold_0/validation \
  --gt_dir  /home/PuMengYu/nnUNet_workspace/preprocessed/Dataset003_Liver/gt_segmentations \
  --img_dir /home/PuMengYu/nnUNet_workspace/raw/Dataset003_Liver/imagesTr \
  --no_vis
```

---

## 3. 补充新节到旧报告（test_report_custom.txt）

> Baseline 和 V2 的 test 报告有 FPV/病灶检出率，但缺连通域分析节。

### Baseline — test report
```bash
cd /home/PuMengYu/nnUNet && python pumengyu/tools/gen_test_report.py \
  --trainer nnUNetTrainer_Baseline \
  --dataset Dataset003_Liver \
  --fold    0
```

### SizeOversampleV2 — test report
```bash
cd /home/PuMengYu/nnUNet && python pumengyu/tools/gen_test_report.py \
  --trainer nnUNetTrainer_SizeOversampleV2 \
  --dataset Dataset003_Liver \
  --fold    0
```

---

## 4. 一次性全部重新生成（按序执行）

```bash
cd /home/PuMengYu/nnUNet

GT=/home/PuMengYu/nnUNet_workspace/preprocessed/Dataset003_Liver/gt_segmentations
IMG=/home/PuMengYu/nnUNet_workspace/raw/Dataset003_Liver/imagesTr
V2=/home/PuMengYu/nnUNet_workspace/results_v2/Dataset003_Liver

# validation reports（全部3个）
python pumengyu/tools/analyasis/eval_fold_report.py --val_dir $V2/nnUNetTrainer_Baseline__nnUNetPlans__3d_fullres/fold_0/validation --gt_dir $GT --img_dir $IMG --no_vis
python pumengyu/tools/analyasis/eval_fold_report.py --val_dir $V2/nnUNetTrainer_SizeOversampleV2__nnUNetPlans__3d_fullres/fold_0/validation --gt_dir $GT --img_dir $IMG --no_vis
python pumengyu/tools/analyasis/eval_fold_report.py --val_dir $V2/nnUNetTrainer_SizeOversampleV3__nnUNetPlans__3d_fullres/fold_0/validation --gt_dir $GT --img_dir $IMG --no_vis

# test reports（Baseline/V2 补节，V3 补生成）
python pumengyu/tools/gen_test_report.py --trainer nnUNetTrainer_Baseline         --dataset Dataset003_Liver --fold 0
python pumengyu/tools/gen_test_report.py --trainer nnUNetTrainer_SizeOversampleV2 --dataset Dataset003_Liver --fold 0
python pumengyu/tools/gen_test_report.py --trainer nnUNetTrainer_SizeOversampleV3 --dataset Dataset003_Liver --fold 0
```
