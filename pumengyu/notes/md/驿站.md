## 不知道放哪里的,先放在这里

---


本地还没有 git 仓库，需要先初始化再关联远程。步骤如下：

第一步：初始化本地 git 仓库
cd /home/PuMengYu/MED_VQA
git init

第二步：关联 GitHub 远程仓库
git remote add origin https://github.com/pu-meng/MED_VQA.git

第三步：查看 GitHub 仓库现有内容
git fetch origin
git branch -r

然后根据情况选择：

情况 A — GitHub 仓库是空的，把本地代码推上去
git commit -m "init: add medical vqa codebase"
git push -u origin main

情况 B — GitHub 仓库已有内容，先拉下来合并：
git pull origin main --allow-unrelated-histories
# 解决冲突后
git push -u origin main


乒乓球我自己接的项目

conda create -n tracknet python=3.8 -y

conda activate tracknet

cd /home/PuMengYu/TrackNetV2-pytorch

pip install -r requirements.txt





## eval 可视化（看失败 case）
# 2026-04-14
# 目的：对 focaltversky_deep_p160_sgd_ratio04 重跑 eval，保存所有 case 可视化
# 重点关注：liver_12（过检）、liver_5、liver_58、liver_94（小肿瘤）

```bash

CUDA_VISIBLE_DEVICES=1 python scripts/eval_twostage.py \
  --medseg_root /home/PuMengYu/medseg_project \
  --preprocessed_root /home/PuMengYu/Task03_Liver_roi \
  --stage1_ckpt /home/PuMengYu/experiments/dynunet_liver_only/train/03-14-01-11-56/best.pt \
  --stage2_ckpt /home/PuMengYu/experiments/twostage/focaltversky_deep_p160_sgd_ratio04/train/04-13-08-47-34/best.pt \
  --stage1_model dynunet \
  --stage2_model dynunet_deep \
  --stage1_patch 144 144 144 \
  --stage2_patch 160 160 160 \
  --val_ratio 0.2 --test_ratio 0.1 --seed 0 \
  --split test \
  --vis_all \
  --prob_threshold 0.3


  
```

---

这个的

#!/bin/bash
# 同步代码和数据到 4090 服务器
# 目标: PuMengYu@219.231.133.120

REMOTE="PuMengYu@219.231.133.120"
REMOTE_HOME="/home/PuMengYu"

# echo "=== [1/5] 同步代码 twostage_medseg ==="
# rsync -avz --progress \
#     /home/pumengyu/twostage_medseg/ \
#     ${REMOTE}:${REMOTE_HOME}/twostage_medseg/

# echo ""
# echo "=== [2/5] 同步代码 medseg_project ==="
# rsync -avz --progress \
#     /home/pumengyu/medseg_project/ \
#     ${REMOTE}:${REMOTE_HOME}/medseg_project/

# echo ""
# echo "=== [3/5] 同步数据 Task03_Liver_roi (30G) ==="
# rsync -avz --progress \
#     /home/pumengyu/Task03_Liver_roi/ \
# #     ${REMOTE}:${REMOTE_HOME}/Task03_Liver_roi/

# echo ""
# echo "=== [4/5] 同步 bbox 缓存 Task03_Liver_json ==="
# rsync -avz --progress \
#     /home/pumengyu/Task03_Liver_json/ \
#     ${REMOTE}:${REMOTE_HOME}/Task03_Liver_json/

# echo ""
# echo "=== [5/5] 同步 checkpoints ==="
# # stage1 checkpoint
# rsync -avz --progress \
#     /home/pumengyu/experiments/dynunet_liver_only/ \
#     ${REMOTE}:${REMOTE_HOME}/experiments/dynunet_liver_only/

# 先在远端建好目录
ssh ${REMOTE} "mkdir -p ${REMOTE_HOME}/nnUNet"

# init checkpoint (stage2)
rsync -avz --progress \
    "/home/pumengyu/nnUNet" \
    ${REMOTE}:${REMOTE_HOME}/nnUNet/

echo ""
echo "=== 同步完成 ==="














